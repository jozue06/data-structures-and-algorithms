'use strict';

const array_adjacent_product = require('../arrays/array_adjacent_product.js');




describe('Insert shift', () => {

  xit('binarySearch() should return the index of 2', () => {

    let matrix = [ 
      [ 1, 2, 1 ], 
      [ 2, 4, 2 ], 
      [ 3, 6, 8 ], 
      [ 7, 8, 1 ], 
    ];
    //array_adjacent_product(matrix);

    expect(array_adjacent_product).toBe(2);
  });
        
  
        
        
});